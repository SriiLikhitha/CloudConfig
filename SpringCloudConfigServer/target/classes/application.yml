server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SriiLikhitha/CloudConfig.git
  application:
    name: config-server

management:
  security:
    enabled: false